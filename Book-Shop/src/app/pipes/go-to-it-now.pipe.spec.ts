import { GoToItNowPipe } from './go-to-it-now.pipe';

describe('GoToItNowPipe', () => {
  it('create an instance', () => {
    const pipe = new GoToItNowPipe();
    expect(pipe).toBeTruthy();
  });
});
