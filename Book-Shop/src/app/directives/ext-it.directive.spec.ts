import { ExtItDirective } from './ext-it.directive';

describe('ExtItDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtItDirective();
    expect(directive).toBeTruthy();
  });
});
